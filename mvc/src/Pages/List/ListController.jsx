import ListView from "./ListView";

const ListController = () => {
  // kullanıcı etkileşimi ile tetiklenecek fonksiyon
  const handleClick = () => {};
  return <ListView handleClick={handleClick} />;
};

export default ListController;
